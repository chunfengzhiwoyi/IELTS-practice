# Local signing configuration template (Android release build).
#
# This file is a TEMPLATE and is safe to commit.
# The real keystore.properties is machine-local and MUST NOT be committed.
# Copy this file to `keystore.properties` (same directory) and fill in the
# values for your own signing identity.
#
# Referenced by: apps/android/app/build.gradle.kts (signingConfigs.release)
# Expected keys (read via java.util.Properties):
storeFile=
storePassword=
keyAlias=
keyPassword=
