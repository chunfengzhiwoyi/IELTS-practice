/**
 * @ielts/core —— 服务端入口
 * ---------------------------------------------------------------------------
 * 仅供 Node 运行环境(Next.js API routes / 云函数)使用。
 * 这里可以安全地 import 带 `import "server-only"` 的模块、读取 `process.env`、
 * 操作数据库与 LLM。
 *
 * 小程序(Taro)构建时不得触碰本文件 —— 否则 `server-only` 会在客户端打包阶段报错。
 */

// 以下为占位，Phase 1+ 从 web 的 lib/ 迁入：
// export * from "./learning/service";
// export * from "./learning/repositories/memory-learning-repository";
// export * from "./speaking/repository";
// export * from "./env";
// export * from "./db/browser";
// export * from "./db/server";
// export * from "./llm/providers/*";
